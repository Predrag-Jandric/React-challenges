import React from "react";
import Button from "./Button";

function BeforeWeStart() {
  return (
    <div>
      <h2>Before we start...</h2>
      <p>
        To get the most out of these challenges, it's helpful to have a basic
        understanding of JSX, components, state, and props. But even if you are
        not sure about these concepts, it will still provide good example
        explanations.
      </p>
      <p>Each project, we will start with new, blank React project, then, just copy and paste my starter code and begin the challenge in your local inveronment.</p>

      <Button>Ok, Let's go !</Button>
    </div>
  );
}

export default BeforeWeStart;
